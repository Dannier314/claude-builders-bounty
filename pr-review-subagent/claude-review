#!/bin/bash
# claude-review — CLI wrapper
# Usage: claude-review --pr https://github.com/owner/repo/pull/123
SCRIPT_DIR="$(cd "$(dirname "$0")" && pwd)"
exec python3 "$SCRIPT_DIR/claude-review.py" "$@"
